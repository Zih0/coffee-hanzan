import { DefaultTheme } from "styled-components";
const color = {
  gray: "#a9a9a9",
  white: "#ffffff",
  black: "#000000",
};

const size = {
  mobile: "(max-width: 768px)",
};

export const theme: DefaultTheme = {
  color,
  size,
};
